<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('./head'); %>
  </head>
  <body>
    <%- include('./header'); %>
    <main>
      <section id="resources-container">
        <h3>List of Resources</h3>
        <a href="/resources/new_resource"><button class="addButton" type="submit">+</button></a>
        <% for(const row of rows) { %>
        <a href="/resources/<%= row.id %>">
          <div class="imageClass" style= "border: 1px solid black ; padding: 20px">
                <img src="<%= row.image %>" width="100" height="100">
                <p><br/></p>
                <p><b>TITLE:</b> <%= row.title %></p>
                <p><b>CATEGORY:</b> <%= row.category_name %></p>
                <a href="<%= row.url %>"><%= row.url %></a>
                <form class="likeButton" action="/resources/<%= row.id %>" method="POST">
                <button class="likeButton" type="submit">Like </button>
                </form>
              </div></a>
              <% } %>
      </section>
    </main>
  </body>
</html>
